import type { PageProps } from '../_types';

export default async function FeedbackPage({}: PageProps) {
	return (
		<div className='container flex gap-6 py-6'>
			<div>what in the world is a roadmap?</div>
		</div>
	);
}
